# Install the package and all dependencies with:
#   pip install -e .
#
# This file mirrors the [project.dependencies] list in pyproject.toml.
torch>=2.0.0
transformers>=4.30.0
datasets>=2.14.0
pyarrow>=12.0.0
numpy>=1.24.0
tqdm>=4.65.0
wandb>=0.17.0
